# A Mutation is an entry point into the object graph.
type PaymentXMutation {
  # Store Card
  storeCard(input:  PaymentXStoredCardInput!):  PaymentXStoredCard!
  # Delete Card
  deleteStoredCard(criteria:  PaymentXStoredCardIDInput!):  PaymentXStoredCard!
}
